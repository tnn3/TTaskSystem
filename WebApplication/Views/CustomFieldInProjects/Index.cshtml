@model IEnumerable<Domain.CustomFieldInProject>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomField)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Project)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomField.CustomFieldId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Project.ProjectId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomFieldInProjectId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomFieldInProjectId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomFieldInProjectId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
