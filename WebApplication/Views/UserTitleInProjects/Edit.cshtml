@model Domain.UserTitleInProject

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<form asp-action="Edit">
    <div class="form-horizontal">
        <h4>UserTitleInProject</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="UserTitleInProjectId" />
        <div class="form-group">
            <label asp-for="ProjectId" class="control-label col-md-2"></label>
            <div class="col-md-10">
                <select asp-for="ProjectId" class="form-control" asp-items="ViewBag.ProjectId"></select>
                <span asp-validation-for="ProjectId" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="TitleId" class="control-label col-md-2"></label>
            <div class="col-md-10">
                <select asp-for="TitleId" class="form-control" asp-items="ViewBag.TitleId"></select>
                <span asp-validation-for="TitleId" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
