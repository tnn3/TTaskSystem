@model WebApplication.Areas.Identity.ViewModels.UserRolesCreateEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<form asp-action="Edit">
    <div class="form-horizontal">
        <h4>User roles</h4>
        <hr />
        <input type="hidden" asp-for="PreviousIdentityUserRole.UserId" />
        <input type="hidden" asp-for="PreviousIdentityUserRole.RoleId" />

        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        @Html.Partial(partialViewName: "_CreateEdit", model: Model)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

