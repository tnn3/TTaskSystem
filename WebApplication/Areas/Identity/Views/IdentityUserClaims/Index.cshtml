@model IEnumerable<AspNetCore.Identity.Uow.Models.IdentityUserClaim>

@{
    ViewData[index: "Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(expression: model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(expression: model => model.ClaimType)
            </th>
            <th>
                @Html.DisplayNameFor(expression: model => model.ClaimValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(expression: modelItem => item.User.Email)
                </td>
                <td>
                    @Html.DisplayFor(expression: modelItem => item.ClaimType)
                </td>
                <td>
                    @Html.DisplayFor(expression: modelItem => item.ClaimValue)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdentityUserClaimId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdentityUserClaimId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdentityUserClaimId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
