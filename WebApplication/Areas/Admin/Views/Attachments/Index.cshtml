@model IEnumerable<Domain.Attachment>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.AttachmentLocation)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UploadedOn)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AttachmentLocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UploadedOn)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AttachmentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AttachmentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AttachmentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
